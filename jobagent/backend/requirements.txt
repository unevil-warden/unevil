fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
pydantic==2.7.1
python-dotenv==1.0.1
httpx==0.27.0
# Real LLM mode (job search, drafting). Mock mode needs none of this.
# web_search + structured outputs require a recent SDK; pin to latest available.
anthropic>=0.40.0
# Real Gmail mode only (read threads, create/send drafts). Mock mode skips these.
google-api-python-client>=2.120.0
google-auth-oauthlib>=1.2.0
google-auth>=2.29.0
